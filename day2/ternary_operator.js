//ternary
let age = 11;
const msg = (age >= 18) ? "You are allowed" : "You are not allowed";
// const isAllowed = (age >= 18) ? true : false; //we should not do it beacause the expression automaticly give you true or false
// const isAllowed = (age >= 18) //try this one

console.log(isAllowed);
console.log(msg);

